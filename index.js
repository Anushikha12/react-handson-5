//1.)what is higher order components?

//--Higher order components are advanced technique for reusing component logic.
//--Basically,its a function that takes a component and returns a new component.
//it is easy to handle
//gets rid of copying the same logic in every component.
//it makes our code more readable.

//2.)What is pure component?

//Pure Component Class compares current state and props with new props and states to decide whether the React component should re-render itself or not.
//PureComponent class are treated as pure components.
//A React component is considered pure if it renders the same output for the same state and props.
